# Findings

- [生产现象] -> 投稿 `567a2b1` 已从失败恢复为待审核 -> 标题为 `Ⱥ���`、演唱为 `46`，歌词含大量 Unicode 替换字符 `�` 和错误解码文本。
- [阶段边界] -> 初稿已经生成且覆盖率为 0% -> 上一处 Phase A 元数据二元组修复有效，本问题发生在输入内容或生成数据质量，不是界面字体渲染。
- [上传清单] -> R2 清单中的标题 `群青町 Arrange Ver. -Bonus Track-`、专辑 `塌陷于38°C的蓝` 与原始路径均为正确 Unicode -> 浏览器清单和对象键未污染。
- [STT] -> R2 `stt/0.json` 中语言为日语，文本和逐字时间戳均为正确日文 -> OpenAI 返回和 WebM 音频不是乱码来源。
- [歌词文件] -> 上传的 `11.群青町.txt` 为 GB18030 编码；现有流程用 UTF-8 加 `errors="replace"` 读取 -> 替换字符在歌词文件解码阶段产生，并覆盖了正确 STT。
- [检测验证] -> `charset-normalizer` 3.4.7 将该对象识别为 `gb18030`，混乱度为 0，并还原出正确标题、词曲署名和日文歌词 -> 可作为服务端确定性解码入口。
- [回归样本] -> 极短的 GB18030 中日混合文本被 `charset-normalizer` 首选为 CP949 -> 回归样本采用包含中日歌词的多行 GB18030 文本，检测结果为 GB18030。
