# 发现

- [用户纠正] -> 前端无法解析是此前草稿修复写回后的数据问题 -> 不把前端结构化行兼容作为根因或生产数据修复替代方案。
- [已知写入] -> 前一任务曾将六份修复 `draft.json` 写回生产 R2，并只进行了对象读回 -> 本次需要补做逐轨工作台行模型验证。
- [直接证据] -> 修复前备份的 `lrc`/`klrc`/`timing_locked` 已被规范化脚本删除 -> 六份写回快照仅保留字符串 `lines`，工作台失去时间轴来源。
- [远端读取] -> Wrangler 在非交互模式无法从多个账户中选择 -> 需先只读确定拥有 `lrc-upload` 的账户，未发生生产写入。
- [离线恢复] -> 对修复前逐字时间轴与修复后正文做字符级 LCS 映射 -> 59 轨可生成与正文行数一致的 LRC/KLRC；`群青町 Arrange Ver. -Bonus Track-` 缺乏任何有效原始时间，保留未对齐。
- [写入范围] -> 将当前生产对象与恢复对象做递归比较 -> 仅 `lrc`、`klrc`、`timing_locked`、`aligned` 四类时间字段变化，正文、曲序、元数据和音频字段均不变。
