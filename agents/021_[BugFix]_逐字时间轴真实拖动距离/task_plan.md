# 逐字时间轴真实拖动距离计划

- [x] 明确水平像素位移必须与可见时间轴使用同一毫秒比例。
- [x] 核对渲染、滚动、缩放、pointer 位移和时间夹紧路径。
- [x] 移除额外增益或不一致的距离归一化。
- [x] 覆盖单像素、多像素与边界拖动回归测试。
- [x] 合并、完整测试并部署统一 Worker。
- [x] 以同一毫秒/像素标尺专项回归覆盖生产构建；浏览器生产控件与边界限制正常加载。
